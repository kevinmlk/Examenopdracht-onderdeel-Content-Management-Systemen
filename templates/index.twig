    {# Neemt de rest van _layout over #}
    {% extends '_layout.twig' %}

    {% block content %}
    <section class="intro-section container mt-5 d-flex justify-content-center">
    {% set videoEntry = craft.entries().section('heroVideoSection').one %}
    <iframe src="{{videoEntry.videoHeader}}controls=0&autoplay=1&mute=1"></iframe>
    
    </section>
    {% endblock %}
